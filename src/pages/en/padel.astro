---
import BaseLayout from "../../layouts/BaseLayout.astro";
import FaqAccordion from "../../components/FaqAccordion.astro";
import PageHero from "../../components/PageHero.astro";
import MembershipSection from "../../components/MembershipSection.astro";
import ConversionBlock from "../../components/ConversionBlock.astro";
import { publicUrl } from "../../i18n/utils";
import { getEntry } from "astro:content";

const lang = 'en' as const;

// SEO + hero + corsi + FAQ gestibili da TinaCMS → src/content/pagine/padel.md (campi _en)
const d = (await getEntry("pagine", "padel"))!.data;
const mdBold = (s: string) => s.replace(/\*\*([^*]+)\*\*/g, '<strong>$1</strong>');
---

<BaseLayout
  lang={lang}
  title={d.title_en ?? d.title}
  description={d.description_en ?? d.description}
  image={d.hero_immagine}
  imageAlt={d.hero_immagine_alt_en ?? d.hero_immagine_alt}
  hideLeadFormInline
>
  <PageHero
    eyebrow={d.hero_eyebrow_en ?? d.hero_eyebrow}
    title={d.hero_titolo_en ?? d.hero_titolo}
    titleAccent={d.hero_titolo_accent_en ?? d.hero_titolo_accent}
    sub={d.hero_sottotitolo_en ?? d.hero_sottotitolo}
    image={d.hero_immagine!}
    imageAlt={d.hero_immagine_alt_en ?? d.hero_immagine_alt!}
    ctas={[
      { label: "Become a Member", href: "#membership" },
      { label: "Book a Court", href: "https://playtomic.io/club-ambrosiano-padel/3c5b3aaa-85e7-41dc-838e-e72899aae1cd?q=PADEL~2024-10-23~~~", external: true, outline: true },
    ]}
  />

  <section class="content-section">
    <div class="content-section__inner">
      <div class="two-col">
        <div class="two-col__text">
          <span class="eyebrow">Padel</span>
          <h2 class="section-title">The sport<br><em>of the moment.</em></h2>
          <p class="body-text">
            Padel is the team game par excellence: doubles, in-house tournaments,
            and an atmosphere that combines competition and fun. At TCA you'll find
            <strong>floodlit courts, FITP-certified coaches</strong> and a community of
            players of every level ready to welcome you.
          </p>
          <button class="btn btn-red" data-open-modal="modal-contatti" style="margin-top: 1.5rem;">Start playing</button>
        </div>
        <div class="two-col__image">
          <img
            src={publicUrl("/padel-middle.avif")}
            alt="TCA padel court"
            class="rounded-img"
          />
        </div>
      </div>
    </div>
  </section>

  <div id="membership" class="anchor-target">
    <MembershipSection lang={lang} />
  </div>

  <section class="content-section content-section--alt">
    <div class="content-section__inner">
      <span class="eyebrow">{d.padel_corsi_eyebrow_en ?? d.padel_corsi_eyebrow}</span>
      <h2 class="section-title">{d.padel_corsi_titolo_en ?? d.padel_corsi_titolo}<br><em>{d.padel_corsi_titolo_accent_en ?? d.padel_corsi_titolo_accent}</em></h2>
      <p class="section-lead" set:html={mdBold(d.padel_corsi_intro_en ?? d.padel_corsi_intro!)} />
      <div class="content-grid content-grid--4">
        {d.padel_corsi_livelli!.map((l) => (
          <div class="content-card content-card--simple">
            <strong>{l.titolo_en ?? l.titolo}</strong>
            <p>{l.testo_en ?? l.testo}</p>
          </div>
        ))}
      </div>
      <div class="section-cta">
        <button class="btn btn-red" data-open-modal="modal-contatti">{d.padel_corsi_cta_label_en ?? d.padel_corsi_cta_label}</button>
      </div>
    </div>
  </section>

  <FaqAccordion lang="en" items={d.faq} />

  <ConversionBlock lang={lang} />

</BaseLayout>

<style>
  .content-section {
    padding: 7vh var(--pad-x);
  }

  .content-section--alt {
    background: var(--color-off-white);
    border-top: 1px solid var(--color-border);
  }

  .content-section__inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .section-title {
    font-family: var(--font-display);
    font-size: clamp(2.2rem, 4vw, 4rem);
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: -0.01em;
    line-height: 0.95;
    margin-bottom: 2.5vh;
  }

  .section-title em { color: var(--color-accent); font-style: normal; }

  .body-text {
    font-family: var(--font-body);
    font-size: clamp(16px, 1.1vw, 20px);
    font-weight: 300;
    line-height: 1.8;
    color: var(--color-text-light);
  }

  .two-col {
    display: flex;
    flex-direction: column;
    gap: 4rem;
  }

  .two-col__text {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .two-col__image {
    margin-top: 2.5rem;
  }

  .rounded-img {
    width: 100%;
    height: clamp(280px, 42vh, 520px);
    object-fit: cover;
    display: block;
  }

  .section-lead {
    font-family: var(--font-body);
    font-size: clamp(16px, 1.1vw, 19px);
    font-weight: 300;
    line-height: 1.75;
    color: var(--color-text-light);
    max-width: none;
    margin-bottom: 4vh;
  }

  .section-cta {
    margin-top: 4vh;
  }

  .content-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .content-grid--4 { grid-template-columns: repeat(4, 1fr); }

  .content-card--simple {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .content-card--simple strong {
    font-family: var(--font-display);
    font-size: clamp(1rem, 1.4vw, 1.4rem);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text);
  }

  .content-card--simple p {
    font-family: var(--font-body);
    font-size: clamp(15px, 1vw, 18px);
    font-weight: 300;
    line-height: 1.7;
    color: var(--color-text-light);
  }

  @media (max-width: 1024px) {
    .content-grid--4 { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 640px) {
    .content-grid--4 { grid-template-columns: 1fr; }
  }

  /* ── Anchor offset per navbar fissa ── */
  .anchor-target {
    scroll-margin-top: calc(var(--header-h) + 1rem);
  }
</style>
