---
import { useTranslations, getLocalePath, getAlternatePath, publicUrl, type Lang } from '../i18n/utils';

interface Props {
  lang?: Lang;
}

const { lang = 'it' } = Astro.props;
const t = useTranslations(lang);
const p = (path: string) => getLocalePath(lang, path);

const base = (import.meta.env.BASE_URL ?? '/').replace(/\/$/, '');
const rawPath = Astro.url.pathname;
const cleanPath = base && rawPath.startsWith(base)
  ? (rawPath.slice(base.length) || '/')
  : rawPath;
const { it: itClean } = getAlternatePath(cleanPath);
const itPath = getLocalePath('it', itClean);
const enPath = getLocalePath('en', itClean);

// ── Mega menu data ────────────────────────────────────────────────
const adultiItems = [
  {
    num: '01', label: t.nav.prepAtletica,
    desc: 'Allenamento in Sala Pesi e Corsi di Gruppo.',
    href: p('/preparazione-atletica'),
    img: publicUrl('/c99ebd5f-16b9-442f-9bb7-2437d5203cd2_TCA+Fitness+5.avif'),
  },
  {
    num: '02', label: t.nav.tennis,
    desc: 'Sedici campi, istruttori federali e tornei per tutti i livelli.',
    href: p('/tennis'),
    img: publicUrl('/3fc4b526-1192-4919-9507-0700f9dbd3bd_TCA+Tennis+2.avif'),
  },
  {
    num: '03', label: t.nav.padel,
    desc: 'Campi coperti e aperti, lezioni con istruttori certificati.',
    href: p('/padel'),
    img: publicUrl('/ba63296a-9191-42dd-ae45-f53124e649ce_Padel.avif'),
  },
];

const juniorItems = [
  {
    num: '01', label: t.nav.scuolaTennis,
    desc: 'Istruttori certificati, piccoli gruppi. Dai 4 anni in su.',
    href: p('/scuola-tennis'),
    img: publicUrl('/ajAJSI1P9HI4UhOS_tenniskidsTCA.avif'),
  },
  {
    num: '02', label: t.nav.agonistica,
    desc: 'Squadre federate, tornei nazionali e internazionali.',
    href: p('/agonistica'),
    img: publicUrl('/0cd25eb6-cb2a-48cf-a65e-2a7107787fba_Web_Square-1PA_1811.avif'),
  },
  {
    num: '03', label: t.nav.summerCamps,
    desc: 'Sport, divertimento e nuove amicizie. Programmi intensivi estate.',
    href: p('/summer-camps'),
    img: publicUrl('/aYszOt0YXLCxVpqZ_TCA_summercamp2026_orizzontale_tennisfemmina_nologhi.avif'),
  },
];

// Voci secondarie (senza mega menu)
const secondaryItems = [
  { label: t.nav.torneoAvvenire, href: p('/torneo-avvenire') },
  { label: t.nav.storia,         href: p('/storia') },
  { label: t.nav.eventi,         href: p('/eventi') },
];

// navItems per il mobile menu — children con img/desc/num per il look scenico
const navItems = [
  { label: t.nav.adulti,  href: null, secondary: false, children: adultiItems },
  { label: t.nav.junior,  href: null, secondary: false, children: juniorItems },
  ...secondaryItems.map(i => ({ ...i, secondary: true, children: [] as typeof adultiItems })),
];
---

<header class="navbar" id="navbar">
  <div class="navbar__inner">
    <!-- Logo -->
    <a class="navbar__logo" href={p('/')} aria-label="Tennis Club Ambrosiano — home">
      <img
        src={publicUrl('/logos/tca-logo.png')}
        alt="Tennis Club Ambrosiano"
        class="navbar__logo-img"
        width="80" height="54"
      />
    </a>

    <!-- Desktop nav -->
    <nav class="navbar__nav" aria-label="Navigazione principale">
      <ul class="navbar__list">

        <!-- ADULTI — mega menu -->
        <li class="navbar__item navbar__item--mega" id="nav-adulti">
          <button class="navbar__link navbar__link--btn" aria-haspopup="true" aria-expanded="false">
            {t.nav.adulti}
            <svg class="navbar__chevron" width="10" height="6" viewBox="0 0 10 6" fill="none">
              <path d="M1 1L5 5L9 1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
          <div class="navbar__mega mega--adulti">
            <div class="mega__inner">
              <ul class="mega__list">
                {adultiItems.map((item) => (
                  <li>
                    <a class="mega__item" href={item.href}>
                      <span class="mega__num">{item.num}</span>
                      <div class="mega__body">
                        <strong class="mega__name">{item.label}</strong>
                        <span class="mega__desc">{item.desc}</span>
                      </div>
                      <span class="mega__arrow" aria-hidden="true">→</span>
                    </a>
                  </li>
                ))}
              </ul>
              <div class="mega__feature">
                {adultiItems.map((item, i) => (
                  <img
                    class={`mega__feat-img mega__feat-img--${i + 1}`}
                    src={item.img}
                    alt={item.label}
                    loading="eager"
                  />
                ))}
                <div class="mega__feat-overlay"></div>
              </div>
            </div>
          </div>
        </li>

        <!-- JUNIOR — mega menu -->
        <li class="navbar__item navbar__item--mega" id="nav-junior">
          <button class="navbar__link navbar__link--btn" aria-haspopup="true" aria-expanded="false">
            {t.nav.junior}
            <svg class="navbar__chevron" width="10" height="6" viewBox="0 0 10 6" fill="none">
              <path d="M1 1L5 5L9 1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
          <div class="navbar__mega mega--junior">
            <div class="mega__inner">
              <ul class="mega__list">
                {juniorItems.map((item) => (
                  <li>
                    <a class="mega__item" href={item.href}>
                      <span class="mega__num">{item.num}</span>
                      <div class="mega__body">
                        <strong class="mega__name">{item.label}</strong>
                        <span class="mega__desc">{item.desc}</span>
                      </div>
                      <span class="mega__arrow" aria-hidden="true">→</span>
                    </a>
                  </li>
                ))}
              </ul>
              <div class="mega__feature">
                {juniorItems.map((item, i) => (
                  <img
                    class={`mega__feat-img mega__feat-img--${i + 1}`}
                    src={item.img}
                    alt={item.label}
                    loading="eager"
                  />
                ))}
                <div class="mega__feat-overlay"></div>
              </div>
            </div>
          </div>
        </li>

        <!-- Voci secondarie -->
        {secondaryItems.map((item) => (
          <li class="navbar__item">
            <a class="navbar__link navbar__link--secondary" href={item.href}>{item.label}</a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="navbar__right">
      <button class="navbar__cta btn btn-red" data-open-modal="modal-contatti">{t.nav.contattaci}</button>
      <a class="navbar__mobile-contact" data-open-modal="modal-contatti">{t.nav.contattaci}</a>
      <button class="navbar__accedi" id="btn-accedi" type="button">ACCEDI</button>

      <div class="lang-switcher" aria-label="Seleziona lingua / Select language">
        <a class={`lang-switcher__link${lang === 'it' ? ' is-active' : ''}`} href={itPath} hreflang="it" aria-label="Italiano">IT</a>
        <span class="lang-switcher__sep" aria-hidden="true">|</span>
        <a class={`lang-switcher__link${lang === 'en' ? ' is-active' : ''}`} href={enPath} hreflang="en" aria-label="English">EN</a>
      </div>

      <button
        class="navbar__burger"
        type="button"
        aria-label={t.nav.openMenu}
        aria-expanded="false"
        aria-controls="g-menu"
        id="burger-btn"
      >
        <span class="burger-bar b1"></span>
        <span class="burger-bar b2"></span>
        <span class="burger-bar b3"></span>
      </button>
    </div>
  </div>
</header>

<!-- Modal Accedi -->
<dialog class="accedi-modal" id="modal-accedi" aria-labelledby="accedi-title">
  <div class="accedi-modal__inner">
    <button class="accedi-modal__close" id="accedi-close" aria-label="Chiudi">
      <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
        <path d="M1 1L17 17M17 1L1 17" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
      </svg>
    </button>
    <p class="accedi-modal__eyebrow">Il tuo Club</p>
    <h2 class="accedi-modal__title" id="accedi-title">Accedi al tuo Account per gestire l'abbonamento e le prenotazioni.</h2>
    <div class="accedi-modal__btns">
      <a class="accedi-modal__btn accedi-modal__btn--red" href="#" target="_blank" rel="noopener">
        ACCEDI DA WEB
      </a>
      <a class="accedi-modal__btn accedi-modal__btn--outline" href="#" target="_blank" rel="noopener">
        ACCEDI DA APP
      </a>
    </div>
  </div>
</dialog>

<!-- Menu fullscreen (mobile) -->
<nav class="g-menu" id="g-menu" aria-label="Menu mobile" aria-hidden="true">
  <div class="g-menu__inner">
    <ul class="g-menu__list">
      {navItems.map((item) => (
        item.children && item.children.length > 0 ? (
          <li class="g-menu__item g-menu__item--accordion">
            <button class="g-menu__link g-menu__accordion-btn" aria-expanded="false">
              {item.label}
              <svg class="g-menu__acc-icon" width="16" height="10" viewBox="0 0 16 10" fill="none">
                <path d="M1 1L8 9L15 1" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>
            <ul class="g-menu__sub">
              {item.children.map((child) => (
                <li>
                  <a class="g-menu__sub-link" href={child.href}>
                    <span class="g-menu__sub-num">{child.num}</span>
                    <div class="g-menu__sub-body">
                      <strong class="g-menu__sub-name">{child.label}</strong>
                      <span class="g-menu__sub-desc">{child.desc}</span>
                    </div>
                    <div class="g-menu__sub-thumb" style={`background-image: url('${child.img}')`}></div>
                  </a>
                </li>
              ))}
            </ul>
          </li>
        ) : (
          <li class={`g-menu__item${item.secondary ? ' g-menu__item--secondary' : ''}`}>
            <a class="g-menu__link" href={item.href}>{item.label}</a>
          </li>
        )
      ))}
    </ul>

    <div class="g-menu__ctas">
      <a class="g-menu__cta-btn g-menu__cta-btn--red btn btn-red" href="#">INIZIA ORA</a>
      <a class="g-menu__cta-btn g-menu__cta-btn--outline" href="#">RICHIEDI LA PROVA</a>
    </div>

    <div class="g-menu__footer">
      <div class="g-menu__lang">
        <a class={`g-menu__lang-link${lang === 'it' ? ' is-active' : ''}`} href={itPath} hreflang="it">IT</a>
        <span class="g-menu__lang-sep" aria-hidden="true">|</span>
        <a class={`g-menu__lang-link${lang === 'en' ? ' is-active' : ''}`} href={enPath} hreflang="en">EN</a>
      </div>
      {t.nav.menuFooter}
    </div>
  </div>
</nav>

<style>
  /* ════════════════════════════════════════════
     NAVBAR
  ════════════════════════════════════════════ */
  .navbar {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 500;
    height: var(--header-h);
    background: #ffffff;
    border-bottom: 1px solid var(--color-border);
  }

  .navbar__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 100%;
    padding: 0 var(--pad-x);
    max-width: var(--max-width);
    margin: 0 auto;
    gap: 1.5rem;
  }

  /* ── Logo ── */
  .navbar__logo { display: block; flex-shrink: 0; line-height: 0; }
  .navbar__logo-img {
    height: calc(var(--header-h) - 16px);
    width: auto; object-fit: contain;
    display: block;
    mix-blend-mode: multiply;
  }

  /* ── Desktop nav ── */
  .navbar__nav { flex: 1; display: flex; }
  .navbar__list {
    list-style: none;
    display: flex;
    align-items: center;
    gap: 0;
  }

  .navbar__item { position: static; }

  .navbar__link,
  .navbar__link--btn {
    font-family: var(--font-display);
    font-size: clamp(14px, 1.14vw, 17px);
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-text);
    text-decoration: none;
    padding: 0 0.9rem;
    height: var(--header-h);
    display: flex;
    align-items: center;
    gap: 5px;
    white-space: nowrap;
    background: none;
    border: none;
    cursor: pointer;
    transition: color 0.18s;
    position: relative;
  }

  /* Underline animato su hover */
  .navbar__link::after,
  .navbar__link--btn::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0.9rem; right: 0.9rem;
    height: 2px;
    background: var(--color-accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.22s cubic-bezier(0.22, 0.68, 0, 1.1);
  }

  .navbar__link:hover::after,
  .navbar__link--btn:hover::after,
  .navbar__item--mega.is-open .navbar__link--btn::after {
    transform: scaleX(1);
  }

  .navbar__link:hover,
  .navbar__link--btn:hover,
  .navbar__item--mega.is-open .navbar__link--btn {
    color: var(--color-accent);
  }

  /* Voci secondarie: più leggere */
  .navbar__link--secondary {
    font-weight: 600;
    color: var(--color-text-light);
    font-size: clamp(14px, 1.02vw, 16px);
    letter-spacing: 0.1em;
  }
  .navbar__link--secondary:hover { color: var(--color-accent); }

  .navbar__chevron {
    transition: transform 0.2s ease;
    flex-shrink: 0;
  }
  .navbar__item--mega.is-open .navbar__chevron { transform: rotate(180deg); }

  /* ════════════════════════════════════════════
     MEGA MENU
  ════════════════════════════════════════════ */
  .navbar__mega {
    position: fixed;
    top: var(--header-h);
    left: 0; right: 0;
    background: #0f1013;
    border-top: 2px solid var(--color-accent);
    box-shadow: 0 20px 60px rgba(0,0,0,0.45);

    /* nascosto di default */
    opacity: 0;
    visibility: hidden;
    transform: translateY(-6px);
    transition: opacity 0.22s ease, transform 0.22s ease, visibility 0.22s ease;
    pointer-events: none;
    z-index: 490;
  }

  .navbar__item--mega.is-open .navbar__mega {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    pointer-events: all;
  }

  /* Layout interno: lista (60%) + immagine featured (40%) */
  .mega__inner {
    display: grid;
    grid-template-columns: 58% 42%;
    max-width: var(--max-width);
    margin: 0 auto;
  }

  /* ── Lista attività ── */
  .mega__list {
    list-style: none;
    padding: 0;
    border-right: 1px solid rgba(255,255,255,0.06);
  }

  .mega__item {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding: 1.6rem var(--pad-x);
    border-bottom: 1px solid rgba(255,255,255,0.06);
    text-decoration: none;
    color: #fff;
    transition: background 0.18s;
    position: relative;
  }

  .mega__item:last-child { border-bottom: none; }

  .mega__item::before {
    content: '';
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 3px;
    background: var(--color-accent);
    transform: scaleY(0);
    transform-origin: top;
    transition: transform 0.22s cubic-bezier(0.22, 0.68, 0, 1.1);
  }

  .mega__item:hover {
    background: rgba(255,255,255,0.03);
  }

  .mega__item:hover::before {
    transform: scaleY(1);
  }

  .mega__num {
    font-family: var(--font-display);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.14em;
    color: var(--color-accent);
    flex-shrink: 0;
    width: 1.6rem;
  }

  .mega__body { flex: 1; min-width: 0; }

  .mega__name {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(1.4rem, 2.2vw, 2.4rem);
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: -0.01em;
    line-height: 1;
    color: rgba(255,255,255,0.85);
    transition: color 0.18s;
  }

  .mega__item:hover .mega__name { color: #fff; }

  .mega__desc {
    display: block;
    font-family: var(--font-body);
    font-size: clamp(11px, 0.85vw, 13px);
    font-weight: 300;
    color: rgba(255,255,255,0.38);
    margin-top: 0.35rem;
    transition: color 0.18s;
  }

  .mega__item:hover .mega__desc { color: rgba(255,255,255,0.6); }

  .mega__arrow {
    font-size: 1.5rem;
    color: var(--color-accent);
    flex-shrink: 0;
    opacity: 0;
    transform: translateX(-10px);
    transition: opacity 0.2s, transform 0.2s;
  }

  .mega__item:hover .mega__arrow {
    opacity: 1;
    transform: translateX(0);
  }

  /* ── Panel immagine featured ── */
  .mega__feature {
    position: relative;
    min-height: 280px;
    overflow: hidden;
    background: #080809;
  }

  .mega__feat-img {
    position: absolute;
    inset: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    object-position: center;
    opacity: 0;
    transform: scale(1.04);
    transition: opacity 0.45s ease, transform 0.6s ease;
  }

  /* Prima immagine visibile di default */
  .mega__feat-img--1 {
    opacity: 0.7;
    transform: scale(1);
  }

  /* CSS :has() — cambia immagine in base all'item in hover */
  .mega--adulti:has(.mega__item:nth-child(1) a:hover) .mega__feat-img--1,
  .mega--junior:has(.mega__item:nth-child(1) a:hover) .mega__feat-img--1 {
    opacity: 0.85; transform: scale(1);
  }
  .mega--adulti:has(.mega__item:nth-child(2) a:hover) .mega__feat-img--1,
  .mega--junior:has(.mega__item:nth-child(2) a:hover) .mega__feat-img--1 { opacity: 0; }
  .mega--adulti:has(.mega__item:nth-child(3) a:hover) .mega__feat-img--1,
  .mega--junior:has(.mega__item:nth-child(3) a:hover) .mega__feat-img--1 { opacity: 0; }

  .mega--adulti:has(.mega__item:nth-child(2) a:hover) .mega__feat-img--2,
  .mega--junior:has(.mega__item:nth-child(2) a:hover) .mega__feat-img--2 {
    opacity: 0.85; transform: scale(1);
  }
  .mega--adulti:has(.mega__item:nth-child(3) a:hover) .mega__feat-img--3,
  .mega--junior:has(.mega__item:nth-child(3) a:hover) .mega__feat-img--3 {
    opacity: 0.85; transform: scale(1);
  }

  .mega__feat-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, rgba(15,16,19,0.4) 0%, transparent 60%);
    pointer-events: none;
  }

  /* ════════════════════════════════════════════
     RIGHT SIDE (CTA + lang + burger)
  ════════════════════════════════════════════ */
  .navbar__right {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-shrink: 0;
  }

  .navbar__cta {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: clamp(12px, 1vw, 14px);
    padding: 7px 18px;
  }

  .lang-switcher {
    display: flex; align-items: center; gap: 4px; flex-shrink: 0;
  }
  .lang-switcher__link {
    font-family: var(--font-display);
    font-size: 11px; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--color-text-light); text-decoration: none;
    transition: color 0.18s;
  }
  .lang-switcher__link:hover { color: var(--color-text); }
  .lang-switcher__link.is-active { color: var(--color-accent); pointer-events: none; }
  .lang-switcher__sep { font-size: 10px; color: var(--color-border); }

  .navbar__mobile-contact { display: none; }

  /* ACCEDI — visibile sempre, stile outline sottile */
  .navbar__accedi {
    font-family: var(--font-body);
    font-size: clamp(12px, 0.9vw, 13px); font-weight: 600;
    letter-spacing: 0.12em; text-transform: uppercase;
    color: var(--color-text);
    background: none;
    border: 1.5px solid var(--color-border);
    padding: 6px 14px; white-space: nowrap;
    cursor: pointer;
    box-shadow: 0 3px 0 0 rgba(0,0,0,0.15);
    transition: border-color 0.18s, color 0.18s, transform 0.1s ease, box-shadow 0.1s ease;
    display: inline-flex; align-items: center;
  }
  .navbar__accedi:hover {
    color: var(--color-accent);
    border-color: var(--color-accent);
    transform: translateY(1px);
    box-shadow: 0 2px 0 0 rgba(139,26,26,0.25);
  }
  .navbar__accedi:active {
    transform: translateY(3px);
    box-shadow: none;
  }

  /* ── Burger ── */
  .navbar__burger {
    display: none; flex-direction: column; justify-content: center;
    gap: 5px; background: none; border: none; cursor: pointer;
    padding: 10px; border-radius: 4px;
    transition: background 0.15s;
  }
  .navbar__burger:hover { background: rgba(0,0,0,0.06); }
  .burger-bar {
    display: block; width: 22px; height: 2px;
    background: var(--color-text); border-radius: 2px;
    transition: transform 0.28s ease, opacity 0.2s ease;
  }
  body.menu-open .b1 { transform: translateY(7px) rotate(45deg); }
  body.menu-open .b2 { opacity: 0; transform: scaleX(0); }
  body.menu-open .b3 { transform: translateY(-7px) rotate(-45deg); }

  /* ════════════════════════════════════════════
     MOBILE BREAKPOINTS
  ════════════════════════════════════════════ */
  @media (max-width: 1023px) {
    .navbar__nav { display: none; }
    .navbar__burger { display: flex; }
  }

  @media (max-width: 640px) {
    .navbar__cta { display: none; }
    .navbar__logo-img { height: calc((var(--header-h) - 16px) * 1.3); }
    .navbar__right { gap: 0.45rem; }
    .lang-switcher__link { font-size: 10px; }
    .navbar__accedi { font-size: 11px; padding: 5px 10px; }
    .navbar__mobile-contact {
      display: inline-flex; align-items: center;
      font-family: var(--font-body);
      font-size: 13px; font-weight: 600;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: #fff; text-decoration: none;
      background: var(--color-accent);
      border: 1.5px solid var(--color-accent);
      padding: 6px 14px; white-space: nowrap;
      box-shadow: 0 3px 0 0 var(--color-accent-dark);
      transition: background 0.18s, border-color 0.18s, transform 0.1s ease, box-shadow 0.1s ease;
      cursor: pointer;
    }
    .navbar__mobile-contact:hover {
      background: var(--color-accent-dark);
      border-color: var(--color-accent-dark);
      transform: translateY(1px);
      box-shadow: 0 2px 0 0 var(--color-accent-dark);
    }
    .navbar__mobile-contact:active {
      transform: translateY(3px);
      box-shadow: none;
    }

  }

  /* ════════════════════════════════════════════
     MENU FULLSCREEN (mobile)
  ════════════════════════════════════════════ */
  .g-menu {
    position: fixed; inset: 0; z-index: 490;
    background: #0f1013;
    pointer-events: none; visibility: hidden; opacity: 0;
    transition: opacity 0.32s ease, visibility 0.32s ease;
  }
  .g-menu.is-open { pointer-events: all; visibility: visible; opacity: 1; }

  .g-menu__inner {
    display: flex; flex-direction: column; height: 100%;
    padding: calc(var(--header-h) + 5vh) var(--pad-x) 5vh;
    overflow-y: auto;
  }

  .g-menu__list { list-style: none; display: flex; flex-direction: column; flex: 1; }

  .g-menu__item { border-bottom: 1px solid rgba(255,255,255,0.06); }

  .g-menu__link {
    font-family: var(--font-display);
    font-size: clamp(28px, 5vw, 3.5rem);
    font-weight: 800; text-transform: uppercase;
    letter-spacing: -0.01em;
    color: rgba(255,255,255,0.45); text-decoration: none;
    line-height: 1.1;
    display: flex; align-items: center; gap: 0.55em;
    padding: 1.8vh 0; width: 100%;
    background: none; border: none; cursor: pointer;
    transition: color 0.18s; text-align: left;
  }

  .g-menu__accordion-btn { color: var(--color-accent); }
  .g-menu__link:hover { color: #fff; }

  .g-menu__acc-icon {
    margin-left: auto; flex-shrink: 0;
    color: rgba(255,255,255,0.25);
    transition: transform 0.28s ease, color 0.18s;
  }
  .g-menu__accordion-btn[aria-expanded="true"] { color: #fff; }
  .g-menu__accordion-btn[aria-expanded="true"] .g-menu__acc-icon {
    transform: rotate(180deg); color: var(--color-accent);
  }

  .g-menu__sub {
    list-style: none; max-height: 0; overflow: hidden;
    transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .g-menu__sub.is-expanded { max-height: 600px; }

  /* Sub-link scenico: num + nome+desc + thumbnail */
  .g-menu__sub-link {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    padding: 1.25vh 0;
    border-bottom: 1px solid rgba(255,255,255,0.05);
    text-decoration: none;
    color: inherit;
    transition: background 0.15s;
    position: relative;
    overflow: hidden;
  }
  .g-menu__sub-link:last-child { border-bottom: none; }

  .g-menu__sub-num {
    font-family: var(--font-display);
    font-size: 11px; font-weight: 700;
    letter-spacing: 0.14em;
    color: var(--color-accent);
    flex-shrink: 0; width: 1.4rem;
    opacity: 0.7;
  }

  .g-menu__sub-body {
    flex: 1; min-width: 0;
  }

  .g-menu__sub-name {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(18px, 4.5vw, 1.7rem);
    font-weight: 800; text-transform: uppercase;
    letter-spacing: -0.01em; line-height: 1;
    color: rgba(255,255,255,0.75);
    transition: color 0.18s;
    white-space: nowrap;
  }

  .g-menu__sub-desc {
    display: block;
    font-family: var(--font-body);
    font-size: clamp(11px, 2.5vw, 13px);
    font-weight: 300;
    color: rgba(255,255,255,0.3);
    margin-top: 0.2rem;
    transition: color 0.18s;
    line-height: 1.35;
  }

  .g-menu__sub-thumb {
    width: 56px; height: 46px;
    flex-shrink: 0;
    background-size: cover;
    background-position: center;
    opacity: 0.35;
    border-radius: 2px;
    transition: opacity 0.25s;
  }

  .g-menu__sub-link:hover .g-menu__sub-name { color: #fff; }
  .g-menu__sub-link:hover .g-menu__sub-desc { color: rgba(255,255,255,0.55); }
  .g-menu__sub-link:hover .g-menu__sub-thumb { opacity: 0.7; }

  /* Voci secondarie nel menu mobile */
  .g-menu__item--secondary { border-bottom: none; }
  .g-menu__item--secondary .g-menu__link {
    font-size: clamp(15px, 3vw, 1.15rem);
    font-weight: 400; letter-spacing: 0.12em;
    color: rgba(255,255,255,0.35); padding: 0.9vh 0;
  }
  .g-menu__item--secondary .g-menu__link:hover { color: rgba(255,255,255,0.75); }
  .g-menu__item--secondary:first-of-type {
    margin-top: 1.5vh; padding-top: 1.5vh;
    border-top: 1px solid rgba(255,255,255,0.06);
  }

  /* CTA mobile */
  .g-menu__ctas {
    display: flex; gap: 1rem; padding: 3vh 0 2vh; flex-wrap: wrap;
  }
  .g-menu__cta-btn {
    font-family: var(--font-display); font-size: 14px; font-weight: 700;
    letter-spacing: 0.12em; text-transform: uppercase; text-decoration: none;
    display: inline-flex; align-items: center;
    padding: 12px 28px; border: 2px solid transparent;
    transition: background 0.18s, border-color 0.18s, color 0.18s;
    cursor: pointer;
  }
  .g-menu__cta-btn--red {
    background: var(--color-accent); border-color: var(--color-accent); color: #fff;
  }
  .g-menu__cta-btn--red:hover {
    background: var(--color-accent-dark); border-color: var(--color-accent-dark);
  }
  .g-menu__cta-btn--outline {
    background: transparent; border-color: rgba(255,255,255,0.35); color: rgba(255,255,255,0.85);
  }
  .g-menu__cta-btn--outline:hover { border-color: #fff; color: #fff; }

  /* ════════════════════════════════════════════
     MODAL ACCEDI
  ════════════════════════════════════════════ */
  .accedi-modal {
    position: fixed;
    inset: 0;
    margin: auto;
    width: min(92vw, 480px);
    max-height: 90vh;
    background: #F4EDE8;
    border: none;
    padding: 0;
    box-shadow: 0 24px 80px rgba(0,0,0,0.22);
    z-index: 600;
  }

  .accedi-modal::backdrop {
    background: rgba(0,0,0,0.55);
    backdrop-filter: blur(3px);
  }

  .accedi-modal__inner {
    padding: 2.8rem 2.4rem 2.4rem;
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .accedi-modal__close {
    position: absolute;
    top: 1.2rem; right: 1.2rem;
    background: none; border: none;
    cursor: pointer; padding: 6px;
    color: rgba(90,60,55,0.5);
    transition: color 0.18s;
  }
  .accedi-modal__close:hover { color: var(--color-accent); }

  .accedi-modal__eyebrow {
    font-family: var(--font-display);
    font-size: 11px; font-weight: 700;
    letter-spacing: 0.18em; text-transform: uppercase;
    color: var(--color-accent);
  }

  .accedi-modal__title {
    font-family: var(--font-display);
    font-size: clamp(1.4rem, 3.5vw, 1.9rem);
    font-weight: 800; text-transform: uppercase;
    letter-spacing: -0.01em; line-height: 1.05;
    color: var(--color-text);
    margin: 0;
  }

  .accedi-modal__btns {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    padding-top: 0.5rem;
    border-top: 1px solid rgba(139,26,26,0.12);
  }

  .accedi-modal__btn {
    font-family: var(--font-display);
    font-size: 13px; font-weight: 700;
    letter-spacing: 0.12em; text-transform: uppercase;
    text-decoration: none;
    display: inline-flex; align-items: center;
    padding: 12px 22px;
    border: 2px solid transparent;
    transition: background 0.18s, border-color 0.18s, color 0.18s;
    white-space: nowrap;
  }

  .accedi-modal__btn--red {
    background: var(--color-accent);
    border-color: var(--color-accent);
    color: #fff;
  }
  .accedi-modal__btn--red:hover {
    background: var(--color-accent-dark);
    border-color: var(--color-accent-dark);
  }

  .accedi-modal__btn--outline {
    background: transparent;
    border-color: rgba(139,26,26,0.25);
    color: var(--color-text);
  }
  .accedi-modal__btn--outline:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
  }

  /* Footer mobile menu */
  .g-menu__footer {
    font-family: var(--font-display); font-size: clamp(11px, 0.6vw, 11px);
    letter-spacing: 0.14em; text-transform: uppercase;
    color: rgba(255,255,255,0.15); margin-top: 2vh; padding-top: 2vh;
    border-top: 1px solid rgba(255,255,255,0.05);
    display: flex; flex-direction: column; gap: 0.75rem;
  }

  .g-menu__lang { display: flex; align-items: center; gap: 6px; }
  .g-menu__lang-link {
    font-family: var(--font-display); font-size: 13px; font-weight: 700;
    letter-spacing: 0.12em; text-transform: uppercase;
    color: rgba(255,255,255,0.35); text-decoration: none; transition: color 0.18s;
  }
  .g-menu__lang-link:hover { color: rgba(255,255,255,0.7); }
  .g-menu__lang-link.is-active { color: var(--color-accent); pointer-events: none; }
  .g-menu__lang-sep { color: rgba(255,255,255,0.12); font-size: 11px; }
</style>

<script>
  // ── Modal Accedi ──
  const accediModal = document.getElementById('modal-accedi') as HTMLDialogElement | null;
  document.getElementById('btn-accedi')?.addEventListener('click', () => accediModal?.showModal());
  document.getElementById('accedi-close')?.addEventListener('click', () => accediModal?.close());
  accediModal?.addEventListener('click', (e) => { if (e.target === accediModal) accediModal.close(); });

  const menu   = document.getElementById('g-menu') as HTMLElement | null;
  const burger = document.getElementById('burger-btn') as HTMLButtonElement | null;

  burger?.addEventListener('click', () => {
    const open = menu?.classList.toggle('is-open');
    document.body.classList.toggle('menu-open', open ?? false);
    burger.setAttribute('aria-expanded', String(!!open));
    menu?.setAttribute('aria-hidden', String(!open));
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      menu?.classList.remove('is-open');
      document.body.classList.remove('menu-open');
      burger?.setAttribute('aria-expanded', 'false');
      menu?.setAttribute('aria-hidden', 'true');
      // chiudi anche i mega menu
      document.querySelectorAll('.navbar__item--mega.is-open').forEach(el => el.classList.remove('is-open'));
    }
  });

  // Accordion mobile
  document.querySelectorAll<HTMLButtonElement>('.g-menu__accordion-btn').forEach((btn) => {
    btn.addEventListener('click', () => {
      const expanded = btn.getAttribute('aria-expanded') === 'true';
      document.querySelectorAll<HTMLButtonElement>('.g-menu__accordion-btn').forEach((b) => {
        b.setAttribute('aria-expanded', 'false');
        (b.nextElementSibling as HTMLElement | null)?.classList.remove('is-expanded');
      });
      if (!expanded) {
        btn.setAttribute('aria-expanded', 'true');
        (btn.nextElementSibling as HTMLElement | null)?.classList.add('is-expanded');
      }
    });
  });

  // Desktop mega menu — click toggle
  document.querySelectorAll<HTMLElement>('.navbar__item--mega').forEach((item) => {
    const btn = item.querySelector<HTMLButtonElement>('.navbar__link--btn');
    btn?.addEventListener('click', () => {
      const isOpen = item.classList.toggle('is-open');
      btn.setAttribute('aria-expanded', String(isOpen));
      // chiudi gli altri
      document.querySelectorAll<HTMLElement>('.navbar__item--mega').forEach((other) => {
        if (other !== item) {
          other.classList.remove('is-open');
          other.querySelector<HTMLButtonElement>('.navbar__link--btn')?.setAttribute('aria-expanded', 'false');
        }
      });
    });

    // Chiudi cliccando fuori
    document.addEventListener('click', (e) => {
      if (!item.contains(e.target as Node)) {
        item.classList.remove('is-open');
        btn?.setAttribute('aria-expanded', 'false');
      }
    });
  });
</script>
